import React from 'react';
import ReactDOM from 'react-dom';
import PageDetail from './components/PageDetail/PageDetail.jsx';

ReactDOM.render(
    <PageDetail />,
    document.getElementById(`root`)
);
